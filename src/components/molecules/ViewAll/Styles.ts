import { StyleSheet } from "react-native";
import { LightTheme as style } from "../../../styles/Theme"

export default StyleSheet.create({
    wrapper : {
        justifyContent: "center",
        alignItems: "center",
        width: 150,
    }
    
})